# Dongda Auto Service LTD — Website (Vite + React + Tailwind)

**Deploy on Vercel**. This project includes a fix for Vercel: `index.html` uses `src/main.jsx` (no leading slash).

## Local dev
```bash
npm install
npm run dev
```

## Build
```bash
npm run build
```
